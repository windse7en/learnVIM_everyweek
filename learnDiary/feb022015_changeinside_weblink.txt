ci):  change inside the parens
ci":  change inside the quotes
di]:  delete inside the []
def index(delete, messge, in, parens)
def test"delete, message, in, quotes"
def square[last, but, not, least]
press dt" to delete everything until the first quote
reset the L to $ and H to ^

Oh, Cool, you have finished!

u:  undo
Ctrl+r: redo, change redo

dotfiles/ store all . start files

weblink for rails ralated cast:
http://vimvasts.rg/
https://upcase.com/vim

plugin:ctrlp, ag, ctrip.vim

locale: show all the locale settings, reset to modify the /etc/environment file

Great website:
  robots.thoughtbot.com
    Seamlessly Navigate Vim and tmux Splits


For the future productive test:
https://robots.thoughtbot.com/running-specs-from-vim-sent-to-tmux-via-tslime

Ctrl+b " - split pane horizontally.
Ctrl+b % - split pane vertically.
Ctrl+b arrow key - switch pane.
Hold Ctrl+b, don't release it and hold one of the arrow keys - resize pane.
Ctrl+b c - (c)reate a new window.
Ctrl+b n - move to the (n)ext window.
Ctrl+b p - move to the (p)revious window.

